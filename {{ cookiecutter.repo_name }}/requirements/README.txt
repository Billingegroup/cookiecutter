# YOU MAY DELETE THIS FILE AFTER SETTING UP DEPENDENCIES!
#
# This directory is where you should place your project dependencies.
# build.txt should contain all Conda packages required to build the project.
# conda.txt should contain all Conda packages (including optional) to install the package via `conda install`
# pip.txt should contain all PyPI packages (including optional) to install the package via `pip install <package-name>`
# test.txt should contain all Conda/PyPI packages required for the testing suite and to ensure all tests pass.
# docs.txt should contain all Conda/PyPI packages required for building the package documentation page.
#
# YOU MAY DELETE THIS FILE AFTER SETTING UP DEPENDENCIES!
